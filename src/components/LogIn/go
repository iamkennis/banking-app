currentAccount = accounts.find( accounts => accounts.username === value);
			
		if (currentAccount?.pin === Number(value)) {

			// Display UI and message
			return <h1>`Welcome back, ${currentAccount.owner.split(' ')[0]}`;</h1>; 
			// containerApp.style.opacity = 100;

			// Clear input fields
			// userLogin.value = userPin.value = '';
			// userPin.blur();

			// Update UI
			// updateUI(currentAccount);
			
		}
